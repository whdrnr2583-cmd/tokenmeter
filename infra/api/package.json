{
  "name": "token-meter-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute token-meter --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute token-meter --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.91.0"
  },
  "dependencies": {
    "hono": "^4.6.13"
  }
}
